import {Router} from 'express';
import Resource from "../utilities/resource";

class BreezeRouter {
  private router = Router();


  use(resource: Resource): void {

    // Path
    // Response
    // Register
    this.router.use();

  }

}

export default (new BreezeRouter());
